"use server";

//import db from "@/app/api/db/connection";

export async function databaseDebug() {
   // await db.dropCollection("players");
   // await db.createCollection("players", { collation: { locale: "en", strength: 1 } });
   // await db.collection("players").createIndex({ osuname: 1 });
   // await db
   //    .collection("players_copy")
   //    .aggregate([{ $out: "players" }])
   //    .toArray();
}
